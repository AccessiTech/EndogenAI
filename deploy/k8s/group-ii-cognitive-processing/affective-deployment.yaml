apiVersion: apps/v1
kind: Deployment
metadata:
  name: affective
  namespace: endogenai-modules
  labels:
    app: affective
    app.kubernetes.io/part-of: endogenai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: affective
  template:
    metadata:
      labels:
        app: affective
    spec:
      runtimeClassName: gvisor
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: affective
        image: endogenai/affective:latest
        ports:
        - containerPort: 8215
        - containerPort: 8315
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /.well-known/agent-card.json
            port: 8215
          initialDelaySeconds: 20
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /.well-known/agent-card.json
            port: 8215
          initialDelaySeconds: 10
          periodSeconds: 10
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir:
          medium: Memory
          sizeLimit: 64Mi
