apiVersion: apps/v1
kind: Deployment
metadata:
  name: executive-agent
  namespace: endogenai-modules
  labels:
    app: executive-agent
    app.kubernetes.io/part-of: endogenai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: executive-agent
  template:
    metadata:
      labels:
        app: executive-agent
    spec:
      runtimeClassName: gvisor
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: executive-agent
        image: endogenai/executive-agent:latest
        ports:
        - containerPort: 8220
        - containerPort: 8320
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /.well-known/agent-card.json
            port: 8220
          initialDelaySeconds: 20
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /.well-known/agent-card.json
            port: 8220
          initialDelaySeconds: 10
          periodSeconds: 10
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir:
          medium: Memory
          sizeLimit: 64Mi
