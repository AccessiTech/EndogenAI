# Phase 9 — Inter-Module Security Review

**Status**: COMPLETE (§9.1–§9.2 implementation reviewed 2026-03-04)  
**Reviewer**: Docs Executive Agent  
**Scope**: 14 module deployments + 2 infrastructure services (gateway, mcp-server)

---

## Review Scope

- [x] Unauthenticated calls inventory
- [x] Exposed ports audit
- [x] Non-root user verification
- [x] readOnlyRootFilesystem compatibility
- [x] NetworkPolicy coverage
- [x] OPA policy coverage (module-capabilities + inter-module-comms)
- [x] mTLS configuration
- [ ] Trivy scan results _(pending — images not yet built)_
- [x] Secrets management review

---

## OPA Policy Review

### Policy files

| File | Status |
| --- | --- |
| `security/policies/helpers.rego` | ✅ Present — `is_infrastructure/1` correctly identifies `mcp-server` and `gateway` |
| `security/policies/module-capabilities.rego` | ✅ Present — default deny; allow only if capability in `agent-card.json` capabilities array |
| `security/policies/inter-module-comms.rego` | ✅ Present — default deny A2A; allow declared consumers, gateway bypass, mcp-server target |

### Data coverage

`security/data/modules.json` was generated by `scripts/gen_opa_data.py` from all 14 `agent-card.json`
files. All 14 modules present:

```
attention-filtering, perception, sensory-input,
affective, episodic-memory, long-term-memory, short-term-memory, working-memory, reasoning,
agent-runtime, executive-agent, motor-output,
learning-adaptation, metacognition
```

Gateway and mcp-server are handled as infrastructure bypasses in `inter-module-comms.rego` and do not
require entries in `modules.json`.

### Test results

9/9 OPA unit tests pass (`opa test security/policies/ security/tests/ -v`):

| Test | Policy | Pass |
| --- | --- | --- |
| `test_allow_declared_capability` | module-capabilities | ✅ |
| `test_deny_undeclared_capability` | module-capabilities | ✅ |
| `test_anomaly_on_undeclared` | module-capabilities | ✅ |
| `test_no_anomaly_when_module_missing` | module-capabilities | ✅ |
| `test_gateway_bypass` | inter-module-comms | ✅ |
| `test_mcp_server_target` | inter-module-comms | ✅ |
| `test_declared_consumer_allowed` | inter-module-comms | ✅ |
| `test_undeclared_caller_denied` | inter-module-comms | ✅ |
| `test_deny_direct` | inter-module-comms | ✅ |

### Enforcement mode

Current setting: `OPA_ENFORCE=false` (audit mode).  
Promote to enforce only after the full test suite passes end-to-end in CI and decision logs show
no unexpected denials.

---

## Dockerfile Security Context Review

Reviewed: all 14 module Dockerfiles + `deploy/docker/base-python.Dockerfile` + `deploy/docker/base-node.Dockerfile` + `infrastructure/mcp/Dockerfile`.

| Check | Status | Notes |
| --- | --- | --- |
| Non-root user | ✅ PASS | All images use UID 65534 (`nobody`); base images set `USER nobody` |
| No package caches | ✅ PASS | `uv sync --no-dev` used in all Python images; `--no-cache` set |
| Minimal base image | ✅ PASS | `endogenai/base-python:3.11` and `endogenai/base-node:20` used |
| No secrets in layers | ✅ PASS | No credentials embedded in any Dockerfile |
| HEALTHCHECK present | ✅ PASS | All module Dockerfiles include HEALTHCHECK against `/.well-known/agent-card.json` |
| gVisor-compatible patterns | ✅ PASS | No `/proc` writes; no raw sockets; no extra capabilities |

---

## Kubernetes YAML Security Context Review

All 16 service Deployment manifests in `deploy/k8s/` were reviewed.

| Security control | Applied to | Status |
| --- | --- | --- |
| `runtimeClassName: gvisor` | All 16 deployments | ✅ All present |
| `runAsNonRoot: true` | All 16 pods | ✅ All present |
| `runAsUser: 65534` | All 16 pods | ✅ All present |
| `seccompProfile: RuntimeDefault` | All 16 pods | ✅ All present |
| `readOnlyRootFilesystem: true` | All 16 containers | ✅ All present |
| `allowPrivilegeEscalation: false` | All 16 containers | ✅ All present |
| `capabilities.drop: ["ALL"]` | All 16 containers | ✅ All present |
| `/tmp` emptyDir volume | All 16 pods | ✅ All present (required for gVisor read-only fs) |
| Resource requests + limits | All 16 containers | ✅ CPU and memory bounds set on all pods |
| Liveness + readiness probes | All 16 containers | ✅ Both probes present on all pods |

---

## gVisor Adoption Status

`deploy/k8s/runtime-class-gvisor.yaml` declares the `gvisor` RuntimeClass. All 16 pod specs set
`spec.template.spec.runtimeClassName: gvisor`.

| Environment | Status |
| --- | --- |
| macOS development | `docker-compose.override.yml` overrides `runtime: runc` — gVisor disabled locally ✅ |
| CI (GitHub Actions) | `runsc` runtime applied to all module container jobs ✅ (configured) |
| Production Kubernetes | All 16 deployments reference `runtimeClassName: gvisor` ✅ |

---

## mTLS Status

**Status: PENDING** — certificates have not yet been generated. The `bash scripts/gen_certs.sh`
script is present and ready to execute. K8s TLS Secret mounts are defined in the deployment manifests.

Action required before production go-live:
1. Run `bash scripts/gen_certs.sh` to generate CA + per-module leaf certificates
2. Create K8s TLS Secrets: `kubectl create secret tls <module>-tls --cert=... --key=...`
3. Verify module services start successfully with mTLS enforced

---

## Trivy Scan Status

**Status: PENDING** — container images have not yet been built. Trivy scans are blocked on a successful
`bash scripts/build_images.sh` run.

Action required before production go-live:
1. Build images: `bash scripts/build_images.sh`
2. Run scans: `trivy image --exit-code 1 --severity HIGH,CRITICAL endogenai/<module>` for all 16 images
3. Run manifest scan: `trivy config deploy/k8s/`
4. Document any accepted waivers in `.trivyignore` with CVE ID and written rationale

---

## Network Policy Coverage

**Default-deny** `NetworkPolicy` is applied to both `endogenai-modules` and `endogenai-infra` namespaces
via `deploy/k8s/network-policy-default-deny.yaml`.

Per-module allow rules are declared in the `deploy/k8s/group-*/` manifests. Coverage: 14/14 modules +
gateway + mcp-server.

---

## Secrets Management Review

| Check | Status |
| --- | --- |
| No committed secrets | ✅ PASS — audit of all source files found no hardcoded credentials |
| Default dev values flagged | ✅ PASS — `JWT_SECRET=dev-secret-change-me` is clearly labeled and must be overridden |
| K8s Secret pattern documented | ✅ PASS — `docs/guides/security.md` covers Secret creation and mount pattern |
| `security/certs/` gitignored | ✅ PASS — `.gitignore` excludes certificate files |

---

## Outstanding Risk Items

| Risk | Severity | Status |
| --- | --- | --- |
| mTLS certificates not generated | HIGH | Pending — blocked until images are built and network validated |
| Trivy scans not run | HIGH | Pending — blocked until images are built |
| OPA in audit mode only | MEDIUM | Intentional deferral — promote after CI validation |
| SPIFFE/SPIRE auto-rotation | LOW | Planned Phase 10 — Phase 9 uses self-signed CA as conscious deferral |
| Keycloak OIDC not configured | LOW | Optional — built-in auth sufficient for Phase 9 |

