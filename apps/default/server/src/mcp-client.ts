// MCP Streamable HTTP client stub — full implementation in §8.1.
export class McpClient {
  constructor(private readonly serverUrl: string) {}

  async initialize(): Promise<void> {
    // stub
  }
}
